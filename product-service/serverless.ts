import type { AWS } from '@serverless/typescript';
import {getProducts, getProductById, createProduct} from '@functions/index';
import { HOST, PORT, DATABASE, PASSWORD, USERNAME } from 'config';

const serverlessConfiguration: AWS = {
  service: 'product-service',
  frameworkVersion: '3',
  plugins: ['serverless-esbuild'],
  provider: {
    name: 'aws',
    runtime: 'nodejs14.x',
    stage: 'dev',
    region: 'eu-west-1',
    apiGateway: {
      minimumCompressionSize: 1024,
      shouldStartNameWithService: true,
    },
    environment: {
      AWS_NODEJS_CONNECTION_REUSE_ENABLED: '1',
      NODE_OPTIONS: '--enable-source-maps --stack-trace-limit=1000',
      PG_HOST: HOST,
      PG_PORT: PORT,
      PG_DATABASE: DATABASE,
      PG_USERNAME: USERNAME,
      PG_PASSWORD: PASSWORD,
    },
  },
  // import the function via paths
  functions: { getProducts, getProductById, createProduct },
  package: { individually: true },
  custom: {
    esbuild: {
      bundle: true,
      minify: false,
      sourcemap: true,
      exclude: ['aws-sdk', 'pg-native', '.config'],
      target: 'node14',
      define: { 'require.resolve': undefined },
      platform: 'node',
      concurrency: 10,
    },
  },
};

module.exports = serverlessConfiguration;
